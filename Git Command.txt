git cheat sheet/ git command

git init

git status

git add "file-name"

git add -A

git commit -m "the Commit message"

git restore "file-name"

git diff --staged

git diff 

git branch

git branch branch name

git checkout branchname

git checkout -b newBranchName
